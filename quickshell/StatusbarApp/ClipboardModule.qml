import QtQuick

// Clipboard history icon. The panel it opens hangs off this button and is wired
// up by the status bar, so there is no action here.
BarButton {
    iconSrc: "../shared/icons/clipboard.svg"
}
